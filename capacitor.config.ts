import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'red-light-green-ligth',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
